pub fn calculate_distance(data: String) -> i64 {
    return 0;
}

fn sort_vectors(data: (Vec<i32>, Vec<i32>)) -> (Vec<i32>, Vec<i32>) {
    let vec1 = Vec::new();
    let vec2 = Vec::new();

    return (vec1, vec2);
}